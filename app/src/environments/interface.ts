export interface EnvironmentConfig {
	envName: string;
  production: boolean;
  api: any;
}

export interface ClientData {
	name: string;
	title: string;
	description: string;
	project: string;
	active: boolean;
}
